import React from "react";
import { createRoot } from "react-dom/client";
import RapSimulatorPrototype from "./rap_simulator_playable.jsx";

createRoot(document.getElementById("root")).render(<RapSimulatorPrototype />);
